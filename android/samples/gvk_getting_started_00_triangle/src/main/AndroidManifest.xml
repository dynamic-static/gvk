<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <uses-feature android:name="android.hardware.vulkan.level" android:version="1" android:required="true" />

    <application
        android:label="gvk-getting-started-00-triangle"
        android:theme="@style/Theme.GvkSample"
        android:hasCode="true">
        <activity
            android:name=".TriangleActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- NOTE : Only needed because our library isn't named libmain.so -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="gvk_getting_started_00_triangle" />
        </activity>
    </application>

</manifest>
